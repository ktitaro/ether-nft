# How to run locally

```sh
# Setup testnet and deploy contract
$ cd dapp
$ yarn install
$ yarn run node # launch local testnet
$ yarn run deploy # compile & deploy contract

# Run website to interact with contract
$ cd website
$ yarn install
$ yarn run dev
```
